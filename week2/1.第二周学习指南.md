# 第二周 PyTorch深度学习实践

## 课程主题

从线性回归到神经网络——完成第一个图像识别AI系统（MNIST）

## 本周目标

通过 PyTorch 完成两个项目：

🧠 项目1：MLP（多层感知机）手写数字识别
👁 项目2：CNN卷积神经网络图像分类（进阶）

## 建立完整深度学习流程认知：

Tensor是什么
Dataset / DataLoader
神经网络训练流程
Loss / Optimizer作用
MLP vs CNN区别
---

# 项目1：MNIST - MLP手写数字识别

---

# Step 1：Run（运行项目）

## 1.1 下载代码

打开：

👉 [https://github.com/pytorch/examples](https://github.com/pytorch/examples)

进入：

```text id="z8m2qp"
mnist/
```

点击：

```text id="q1v8mk"
Code → Download ZIP
```

解压后得到：

```text id="p3x7qn"
examples-main/
```

---

## 1.2 打开项目

VS Code：

```text id="w2k9mp"
File → Open Folder
```

选择：

```text id="r8x1qd"
examples-main/
```

---

## 1.3 安装依赖

打开 Terminal：

```bash id="t6m2qp"
conda activate ai-summer
pip install torch torchvision matplotlib
```

---

## 1.4 运行MNIST

进入目录：

```bash id="c7x9mp"
cd mnist
python main.py
```

---

## ✔ 验收

看到：

* downloading MNIST
* training logs
* accuracy输出

---

# Step 2：Understand（理解代码）

重点3件事：

* Dataset（数据从哪来）
* Model（MLP结构）
* Training loop（训练循环）

---

# Step 3：Modify（修改实验）

做3个实验：

---

## 实验1：改隐藏层

```python id="h8q2mp"
hidden_size = 128 → 256 → 512
```

---

## 实验2：改学习率

```python id="n4x9qp"
lr = 0.01 → 0.001
```

---

## 实验3：减少数据量

只用：

```text id="k2m8xp"
subset MNIST
```

---

✔ 输出：

对比 accuracy 表

---

# Step 4：Build（重写MLP —— 详细执行版）

> ⚠️ 这一部分是本周最重要能力

目标：

👉 不看官方代码，自己从0写一个MNIST训练器

---

# 4.1 创建新项目

在 VS Code 创建：

```text id="a9x2mp"
week02_mnist/
```

结构：

```text id="b3k9qp"
week02_mnist/

├── data/
├── model/
├── train/
├── utils/
└── main.py
```

---

# 4.2 创建模型文件

创建：

```text id="c7p3mq"
model/mlp.py
```

输入：

```python id="m1x9qp"
import torch
import torch.nn as nn

class MLP(nn.Module):
    def __init__(self):
        super().__init__()
        self.model = nn.Sequential(
            nn.Flatten(),
            nn.Linear(28*28, 128),
            nn.ReLU(),
            nn.Linear(128, 10)
        )

    def forward(self, x):
        return self.model(x)
```

---

# 4.3 创建数据加载

创建：

```text id="d8x2mp"
data/dataset.py
```

输入：

```python id="p9x3qp"
import torch
from torchvision import datasets, transforms
from torch.utils.data import DataLoader

def get_data(batch_size=64):
    transform = transforms.ToTensor()

    train_data = datasets.MNIST(
        root="./data",
        train=True,
        download=True,
        transform=transform
    )

    test_data = datasets.MNIST(
        root="./data",
        train=False,
        download=True,
        transform=transform
    )

    train_loader = DataLoader(train_data, batch_size=batch_size, shuffle=True)
    test_loader = DataLoader(test_data, batch_size=batch_size, shuffle=False)

    return train_loader, test_loader
```

---

# 4.4 创建训练代码（核心）

创建：

```text id="e4k9mp"
train/train.py
```

输入：

```python id="q8x1qp"
import torch
import torch.nn as nn

def train(model, train_loader, optimizer, criterion, device):
    model.train()

    total_loss = 0

    for x, y in train_loader:
        x, y = x.to(device), y.to(device)

        # forward
        outputs = model(x)
        loss = criterion(outputs, y)

        # backward
        optimizer.zero_grad()
        loss.backward()
        optimizer.step()

        total_loss += loss.item()

    return total_loss / len(train_loader)
```

---

# 4.5 创建测试代码

创建：

```text id="f2x8mp"
train/test.py
```

输入：

```python id="r7x9qp"
import torch

def test(model, test_loader, device):
    model.eval()
    correct = 0
    total = 0

    with torch.no_grad():
        for x, y in test_loader:
            x, y = x.to(device), y.to(device)

            outputs = model(x)
            _, predicted = torch.max(outputs, 1)

            total += y.size(0)
            correct += (predicted == y).sum().item()

    acc = correct / total
    return acc
```

---

# 4.6 创建主程序（整合）

创建：

```text id="g6x2mp"
main.py
```

输入：

```python id="u8x9qp"
import torch
import torch.nn as nn
import torch.optim as optim

from model.mlp import MLP
from data.dataset import get_data
from train.train import train
from train.test import test

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

# data
train_loader, test_loader = get_data()

# model
model = MLP().to(device)

# loss & optimizer
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)

# training loop
epochs = 3

for epoch in range(epochs):
    loss = train(model, train_loader, optimizer, criterion, device)
    acc = test(model, test_loader, device)

    print(f"Epoch {epoch}: Loss={loss:.4f}, Acc={acc:.4f}")
```

---

# 4.7 运行项目

```bash id="h9x2qp"
python main.py
```

---

## ✔ Step 4 完成标准

你必须做到：

* 能解释每个文件作用
* 能独立跑通训练
* 能看到 accuracy 输出

---

# Step 5：Summarize（总结）

写 README：

必须包含：

* MLP结构
* 数据来源
* 训练流程
* accuracy结果
* 你做的改动

---

很好，这个修改其实是**课程结构上的一个关键优化**，会让第二周更“渐进式”，避免学生在同一周内认知跳跃过大。

我帮你把项目2改成一个更合理的版本：

> 👉 CNN不是独立项目，而是 **“在MLP项目基础上的结构升级实验”**


---

# 项目2：CNN进阶实验（基于MLP的结构升级）

---

# Step 1：Run（理解基线）

## 目标

确认你已经完成：

* MLP版本 MNIST 可正常运行
* accuracy正常输出

---

## 基线模型

来自：

```text id="base_mlp"
week02_mnist/
```

结构：

```python id="mlp_base"
Flatten → Linear → ReLU → Linear
```

---

# Step 2：Understand（理解CNN替换点）

## ❗关键认知

你不是在“换项目”，而是在做：

> ✔ Model Replacement（模型替换实验）

---

## MLP vs CNN 核心区别

| 模型  | 输入方式    | 特点     |
| --- | ------- | ------ |
| MLP | flatten | 忽略空间结构 |
| CNN | 2D图像    | 保留空间结构 |

---

## CNN核心结构

f(x) = Conv2D(x) → ReLU → MaxPool → Flatten → Linear

---

# Step 3：Modify（替换MLP为CNN）

## 🎯 关键任务

👉 你不写新项目，只改一件事：

> 替换 model/mlp.py → CNN版本

---

# 3.1 修改模型文件

打开：

```text id="model_file"
week02_mnist/model/mlp.py
```

---

# 3.2 替换为CNN模型

```python id="cnn_replace"
import torch
import torch.nn as nn

class CNN(nn.Module):
    def __init__(self):
        super().__init__()

        self.model = nn.Sequential(
            nn.Conv2d(1, 16, kernel_size=3, padding=1),
            nn.ReLU(),
            nn.MaxPool2d(2),

            nn.Conv2d(16, 32, kernel_size=3, padding=1),
            nn.ReLU(),
            nn.MaxPool2d(2),

            nn.Flatten(),

            nn.Linear(32 * 7 * 7, 128),
            nn.ReLU(),
            nn.Linear(128, 10)
        )

    def forward(self, x):
        return self.model(x)
```

---

# 3.3 修改 main.py（只改一行）

原来：

```python id="old_model"
from model.mlp import MLP
model = MLP()
```

---

改为：

```python id="new_model"
from model.mlp import CNN
model = CNN()
```

---

# ✔ 验收标准

你必须看到：

* CNN可以正常训练
* loss下降
* accuracy输出

---

# Step 4：Build（对比实验）

## 🎯 关键任务：MLP vs CNN对比

你现在做的是：

> 同一个数据集 + 两个模型

---

## 实验1：MLP结果

记录：

* accuracy
* loss

---

## 实验2：CNN结果

记录：

* accuracy
* loss

---

## 输出表格：

| 模型  | Accuracy |
| --- | -------- |
| MLP | xx%      |
| CNN | xx%      |

---

## 你要理解：

👉 CNN通常更好，因为：

* 保留空间结构
* 参数共享
* 局部感知

---

# Step 5：Summarize（总结）

必须回答：

---

## 1. 为什么CNN比MLP好？

---

## 2. CNN做了什么改进？

* feature extraction
* spatial structure
* parameter sharing

---

## 3. 什么时候用CNN？

* 图像
* 语音
* 空间数据

---

# 📦 最终项目结构（统一版）

```text id="final_structure"
week02_mnist/

├── model/
│   └── mlp.py   (MLP + CNN切换)
├── data/
├── train/
├── main.py
├── figures/
└── README.md
```

